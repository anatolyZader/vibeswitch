# TDD — Spec-First TDD

Start spec-first TDD. Replace `[path-to-spec]` with the actual spec file (e.g. `docs/specs/spec-validateEmail.md`).

## Instruction

I'm starting spec-first TDD. The spec is at **[path-to-spec]**.

1. **Read the spec** and write tests that cover every input/output pair, edge case, and error case in the spec. Use the template and patterns in `docs/specs/SPEC-TEMPLATE.md` and `docs/TDD-PLAN-AND-WORKFLOW.md`. Do **not** implement yet — tests must fail first (Red phase).
2. **Run the tests** and confirm they fail for the right reason.
3. After I commit the tests, **implement** to pass all tests. Do not modify the tests. Iterate until all tests pass (Green phase).

Full workflow: see `docs/2026-02-19_17-38-spec-first-tdd-workflow.md` (or latest `docs/*-spec-first-tdd-workflow.md`).
