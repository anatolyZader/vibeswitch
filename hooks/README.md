# hooks/ (deprecated)

Hook scripts now live in **`.cursor/hooks/`** only. This folder is kept so the `hooks/` path exists for any legacy references; it is otherwise unused. See `.cursor/hooks/README.md` and [Agent loop and stop hook](docs/AGENT-LOOP-STOP-HOOK.md).
