/**
 * Port: Generate correlation IDs for agent jobs.
 * Implemented by infrastructure/adapters/agentsCryptoIdAdapter.js.
 *
 * @interface
 * @typedef {Object} IAgentsIdPort
 * @property {function(): string} generateCorrelationId - Returns a unique correlation id (e.g. UUID)
 */

module.exports = {};
